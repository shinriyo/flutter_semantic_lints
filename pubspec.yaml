name: flutter_semantic_lints
description: Custom lint rules that detect meaningless, conflicting, or no-effect Flutter code.
version: 0.1.0
repository: https://github.com/shinriyo/flutter_semantic_lints
issue_tracker: https://github.com/shinriyo/flutter_semantic_lints/issues

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  analyzer: ^8.0.0
  custom_lint_builder: ^0.8.1

dev_dependencies:
  custom_lint: ^0.8.1
